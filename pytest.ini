[pytest]
# Only the real suite.  Probe scripts under demo/output/probes/ (e.g.
# probe5_module_test.py, run_external_test.py) match pytest's default
# "*_test.py" pattern, run system-level side effects at import time and
# terminate the interpreter, which killed a bare "pytest -q" from the root.
testpaths = tests
